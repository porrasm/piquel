# TODO

## Pending tasks

- add support for named schemas (currently only public schema is supported)
- add support for overriding the column type in the schema generation config. E.g., for user defined types.
